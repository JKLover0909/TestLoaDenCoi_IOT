# AGENTS.md — TestLoaDenCoi_IOT

## Rules for AI Agents
1. Khi chỉnh sửa `code.py`, đảm bảo kiểm tra kết nối MQTT và xử lý ngoại lệ đầy đủ.
2. Không cứng mã hóa thông tin xác thực nhạy cảm trong mã nguồn công khai.
3. Giữ cấu trúc file nhẹ và tương thích với `paho-mqtt`.
